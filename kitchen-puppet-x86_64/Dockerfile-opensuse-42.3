FROM mesaguy/opensuse-42.3-kitchen-base-x86_64:latest

# Install puppet for kitchen
RUN zypper --non-interactive install puppet && \
# Clean (purge) the repo cache
zypper --non-interactive clean --all && \
# Purge logs
rm -rf /var/log/* /var/run/log/journal

# Image metadata
LABEL org.label-schema.vendor="MesaGuy" \
      org.label-schema.name="OpenSUSE Linux 42.3 kitchen-puppet" \
      org.label-schema.description="Image boots OpenSUSE Linux 42.3 via systemd configured for kitchen puppet testing" \
      com.mesaguy.operatingsystem.eol="true"
