FROM mesaguy/fedora-rawhide-kitchen-base-x86_64:latest

# Make a file to force new layer
RUN touch /root/ANSIBLE && \
# Purge logs
rm -rf /var/log/* /var/run/log/journal

# Image metadata
LABEL org.label-schema.vendor="MesaGuy" \
      org.label-schema.name="Fedora Linux rawhide kitchen-ansible" \
      org.label-schema.description="Image boots Fedora Linux rawhide via systemd configured for kitchen ansible testing" \
      com.mesaguy.operatingsystem.eol="true"
